name: material_ribbon
description: Material 3 ribbon components for Flutter desktops, webs, phones, and adaptive layouts.
version: 1.1.0
homepage: https://github.com/Heyairu/flutter_material_ribbon
repository: https://github.com/Heyairu/flutter_material_ribbon
topics:
  - flutter
  - material-design
  - ribbon
  - desktop
  - web

environment:
  sdk: ^3.12.1

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  flutter_lints: ^6.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
